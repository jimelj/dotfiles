- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Work
    - ~/WebDev/Tutorials

- shell:
    -
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true

