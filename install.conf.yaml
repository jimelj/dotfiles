- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim: vim
    ~/.zshrc: zshrc

- create:
    - ~/Work
    - ~/WebDev/Tutorials

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
